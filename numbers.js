// old 
var a = 1;

// new
const a = 1;
// const variables are immutable, they cant change

// let
let b = 2;
// let variable you can change, mutable (usable in loops)

const multiple = 9 * 9;
const divide = 9 / 9;
const addition = 4 + 4;
const subtraction = 4 - 2;
const modulus = 10 % 4;

// variable names should be camel case
const randomNumber = Math.random();

const randomWholeNumber = Math.round(Math.random() * 10);

// typeof() will check datatype